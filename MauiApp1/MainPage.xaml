<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <VerticalStackLayout Padding="20">
        <Label 
        Text="What Would They Do?" 
        FontSize="35" 
        FontAttributes="Bold" 
        HorizontalOptions="Center"
        Margin="0,0,0,20"/>

        <Grid Padding="20" HorizontalOptions="Center" VerticalOptions="Center" Margin="0,100,0,0">
            
            <!-- Define rows and columns for the grid -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <!-- First button -->
            <Button 
                Text="MY CHARACTERS" 
                FontSize="30"
                TextColor="White"
                Clicked="OnAddCharacterClicked"
                WidthRequest="300"
                HeightRequest="300"
                BackgroundColor="DarkGreen"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Grid.Row="0"
                Grid.Column="0" />

            <!-- Second button -->
            <Button 
                Text="CHARTS" 
                FontSize="30"
                TextColor="White"
                Clicked="OnQuestionsListClicked"
                WidthRequest="300"
                HeightRequest="300"
                BackgroundColor="Purple"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Grid.Row="0"
                Grid.Column="1" />

        </Grid>
    </VerticalStackLayout>

</ContentPage>
